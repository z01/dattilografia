<!--

function posar_correu( username )
{
	var domain = "dattilocorso.com";
	var atsign = "@";
	var addr = username + atsign + domain;
	document.write("<" + "a" + " " + "href=" + "mail" + "to:" + addr + ">" + addr + "<\/a>");
}

function correu( username )
{
	var domain = "dattilocorso.com";
	var atsign = "@";
	var addr = username + atsign + domain;
	return(addr);
}

-->